# 邻里云 · 物业管理系统

这是根据 PMA_Login_Architecture_Skill.md 实现的本地 Windows 桌面应用。使用 Python Tkinter 和 SQLite，无第三方依赖；当前电脑已验证具备运行环境。

## 启动

双击 `start.bat`，或在本目录运行 `python app.py`。首次启动设置超级管理员账号，之后该初始化入口关闭。没有默认密码和公开注册入口。

## 使用顺序

1. 设置管理员，选择“超级管理员”登录。
2. 在“房屋档案”添加房屋。
3. 在“用户管理”创建账号，设置初始密码、姓名、角色和部门；业主必须关联房屋。支持为同一账号分配多个角色。
4. 新账号手动选择已分配的角色登录，强制修改初始密码，再重新登录。
5. 物业主管维护房屋、处理报修；财务创建账单、确认线下收款；业主仅查看自己房屋的账单并提交报修。

管理员可锁定、解锁和重置其他账号密码。登录名不可修改；本版本暂不提供已有账号角色编辑、删除或多房屋绑定。更换身份需退出后重新选择登录。

## 认证与数据

- 密码使用随机盐和 PBKDF2-HMAC-SHA256（310,000 次迭代）保存，不保存明文。
- 连续 5 次密码错误锁定账号，正确密码会清零密码错误计数；角色选错另记审计。
- 首次登录获得的会话仅允许改密；改密、管理员锁定、重置密码均使相关旧会话失效。
- 每个服务操作校验会话角色，业主查询在 SQL 中按房屋过滤。
- 工厂按角色生成菜单和工作台；不存在公开注册 API。
- SQLite 文件在程序目录 `data/pma.sqlite3`，重启后数据保留。关闭程序后复制 data 目录即可备份，恢复时关闭程序并替换该目录。
- 审计显示最近 500 次登录结果，数据库保留全部记录；时间为 UTC。
- 首次管理员属于本地部署初始化例外，由自身记录作为创建人。

## 验证

运行 `python test_system.py`，验证激活限制、多角色权限、业主数据隔离、五次错误锁定、解锁、重置密码、审计、密码存储及四种角色页面构建。

## 适用范围

这是可运行的单机基础版，包含真实持久化和基本业务操作。费用登记不对接支付平台。未实现网络服务端、多人联网、安装包、自动备份和账号找回。SQLite 文件的读取和修改依赖操作系统文件权限，不能用客户端角色隔离防御具有本机数据库文件访问权的人员。正式多人部署应增加独立服务端及受保护数据库。
